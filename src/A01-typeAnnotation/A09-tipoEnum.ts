enum Cores { //pode se setar a posição, caso queira
  VERMELHO, //0
  AZUL = 5, //5
  AMARELO = 200, //posicao setada manualmente
  ROXO = 'ROXO', //Caso queira continuar a posição numeral, precisa iniciar o proximo manualmente
}

console.log(Cores.AZUL);
console.log(Cores[0]);
